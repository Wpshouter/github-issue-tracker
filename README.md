
- Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot. 
    - 1️⃣ What is the difference between var, let, and const?
          var is hoisted to the top of the scope we shall not use it. Let is used when we need to resassign the vairable value. Const value is not reassigable.
    - 2️⃣ What is the spread operator (...)?
          this spread operator is used to lay the array elements in context. It can be used to merge two array or objects
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
          -foreach fires for each of them. Map function modify each elment using a function returns a new array with transformed elements. Filter function is used to check the array item based on a condition.
    - 4️⃣ What is an arrow function?
        - arror function is shorthand function => {} 
    - 5️⃣ What are template literals?
        - `` these are the sign of the template literals we can use this to create mutiline strings. and can use variable using ${}. 
